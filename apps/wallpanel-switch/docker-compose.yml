services:
  wallpanel-switch:
    build:
      context: ../..
      dockerfile: apps/wallpanel-switch/Dockerfile
    restart: unless-stopped
    volumes:
      - wallpanel_switch-data:/app/data
    environment:
      WALLPANEL_SWITCH_MQTT__HOST: mosquitto
    depends_on:
      - mosquitto

  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - '127.0.0.1:1883:1883'
    volumes:
      - mosquitto-data:/mosquitto/data
      - mosquitto-config:/mosquitto/config

volumes:
  wallpanel_switch-data:
  mosquitto-data:
  mosquitto-config:
