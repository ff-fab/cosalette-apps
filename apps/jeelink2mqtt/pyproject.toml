[project]
name = "jeelink2mqtt"
version = "0.1.3"
description = "A smart home app to read in values of Jeelink temperature and humidity sensors."
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
authors = [
    { name = "Fabian Koerner", email = "mail@fabiankoerner.com" }
]

dependencies = [
    "cosalette>=0.5.8,<0.6",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "pylacrosse>=0.4",
]

[project.scripts]
jeelink2mqtt = "jeelink2mqtt.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packages/src/jeelink2mqtt"]

[tool.pytest.ini_options]
testpaths = ["packages/tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--strict-config",
]
markers = [
    "unit: Unit tests (fast, no external dependencies)",
    "integration: Integration tests (require mock servers)",
    "slow: Tests that take > 1 second",
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["packages/src/jeelink2mqtt"]
branch = true
omit = [
    "*/__pycache__/*",
    "*/tests/*",
    "*/main.py",  # Entry point — covered by integration tests
    "*/app.py",   # Composition root — covered by integration tests
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@abstractmethod",
    "def __repr__",
    'if __name__ == "__main__":',
    "^\\s*\\.\\.\\.$",  # Protocol ellipsis stubs
]
fail_under = 80
show_missing = true
skip_covered = false

[tool.coverage.json]
output = "coverage.json"

[tool.coverage.html]
directory = "htmlcov"

[tool.coverage.xml]
output = "coverage.xml"

# App-specific isort config — root ruff config handles everything else
# Explicitly inherit root's [tool.ruff] select (and everything else) via
# `extend` — Ruff's config discovery uses the *nearest* pyproject.toml as the
# sole config source and does not merge upward on its own, so without this,
# any [tool.ruff.*] table here (even just isort) would silently shadow root's
# `select` and fall back to Ruff's own default rule set instead (cap-4lc:
# surfaced by the ruff 0.16 default-rule-set expansion from ~59 to ~413).
[tool.ruff]
extend = "../../pyproject.toml"

[tool.ruff.lint.isort]
known-first-party = ["jeelink2mqtt"]
