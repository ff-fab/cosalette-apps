# Canonical settings profile for `task airthings2mqtt:schema:generate`.
#
# Not used at runtime and contains no real credentials. It supplies the required
# BLE address so schema resolution can expand the settings-derived device name.
AIRTHINGS2MQTT_DEVICE_MAC=AA:BB:CC:DD:EE:FF
AIRTHINGS2MQTT_DEVICE_NAME=airthings
