services:
  airthings2mqtt:
    build:
      context: ../..
      dockerfile: apps/airthings2mqtt/Dockerfile
    restart: unless-stopped
    volumes:
      - airthings2mqtt-data:/app/data
    environment:
      AIRTHINGS2MQTT_MQTT__HOST: mosquitto
    depends_on:
      - mosquitto

  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - '127.0.0.1:1883:1883'
    volumes:
      - mosquitto-data:/mosquitto/data
      - mosquitto-config:/mosquitto/config

volumes:
  airthings2mqtt-data:
  mosquitto-data:
  mosquitto-config:
