# Representative environment profile used by schema tooling
# (task wiz2mqtt:schema:check / :schema:generate --resolve-settings).
# Not a real deployment .env — committed, no secrets.

WIZ2MQTT_MQTT__HOST=localhost
WIZ2MQTT_MQTT__PORT=1883
WIZ2MQTT_MQTT__TOPIC_PREFIX=wiz2mqtt
WIZ2MQTT_LOGGING__LEVEL=INFO
WIZ2MQTT_LOGGING__FORMAT=json
