services:
  wallpanel-cntrl:
    build:
      context: ../..
      dockerfile: apps/wallpanel-cntrl/Dockerfile
    restart: unless-stopped
    volumes:
      - wallpanel_cntrl-data:/app/data
    environment:
      WALLPANEL_CNTRL_MQTT__HOST: mosquitto
    depends_on:
      - mosquitto

  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - '127.0.0.1:1883:1883'
    volumes:
      - mosquitto-data:/mosquitto/data
      - mosquitto-config:/mosquitto/config

volumes:
  wallpanel_cntrl-data:
  mosquitto-data:
  mosquitto-config:
