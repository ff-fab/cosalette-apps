{
  // Read AGENTS.md as the canonical instruction file. It is shared with Claude Code
  // (via the @AGENTS.md import in CLAUDE.md) and Kilo, which both read it natively.
  "chat.useAgentsMdFile": true,

  // Auto-approve safe terminal commands for Copilot agent mode.
  // Docs: https://code.visualstudio.com/docs/copilot/copilot-settings#_agent-settings
  "chat.tools.terminal.autoApprove": {
    // Read-only / inspection
    "cat": true,
    "head": true,
    "tail": true,
    "wc": true,
    "awk": true,
    "ls": true,
    "tree": true,
    "pwd": true,
    "grep": true,
    "diff": true,
    "which": true,
    "echo": true,
    "cd": true,
    "mkdir": true,

    // Version control
    "git": true,
    //"gh": true,

    // Task runner (all targets defined in Taskfile.yml)
    "task": true,

    // Python (via uv, not bare python)
    "uv": true,

    // Documentation
    "zensical": true,

    // Planning / project management
    "bd": true,

    // Demos
    "showboat": true
  }
}
